import { Member } from '../../types/member';

const initialization = size => {
  return [...new Array(size)].map((val, index) => new Member(index));

  return population;
};

export { initialization };
